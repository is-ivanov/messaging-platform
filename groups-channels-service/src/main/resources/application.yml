server:
  port: 8082

spring:
  application:
    name: groups-channels-service
  datasource:
    url: jdbc:h2:file:./data/groups-db;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true          # http://localhost:8082/h2-console
      path: /h2-console
